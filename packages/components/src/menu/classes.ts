import { generateUtilityClasses } from '@mui/material'

export const menuClasses = generateUtilityClasses('menu', [
  'icon',
  'menuItem',
  'showMenuItem',
  'modal',
  'isDesktop',
  'isMobile',
])
