'use client'

import { styled } from '@lukasbriza/styles'

export const Main = styled('main')(() => ({
  overflowX: 'hidden',
  width: '100%',
  position: 'absolute',
  top: 0,
  left: 0,
  zIndex: 0,
}))
