# NEED TO DEFINE THESE VARIABLES IN .ENV #
#########################################
# - NEXT_PUBLIC_GITHUB
# - NEXT_PUBLIC_MAIL
# - DATABASE_URL
# - API_KEY
# - API_URL
# - DATABASE_PORT
# - BACKEND_PORT
# - FRONTEND_PORT
# - POSTGRES_PASSWORD
# - POSTGRES_USER
# - DATABASE_NAME
# - POSTGRES_HOST
# - DATABASE_PATH
# - NODE_ENV
# - MAIL_RECIPIENT
# - MAIL_MODULE_HOST
# - MAIL_MODULE_USER
# - MAIL_MODULE_PASSPHRASE

networks:
  app-prod:
    driver: bridge

services:
  frontend:
    build:
      context: .
      dockerfile: docker/nextjs/Dockerfile
      args:
        - APP=frontend
        - TAG=base-latest
        - NODE_ENV=production
    image: lbriza/coincrusade-runner-game:fe-prod
    container_name: coin-crusade-game_fe-prod
    restart: always
    ports:
      - ${FRONTEND_PORT}:3000
    environment:
      - NODE_ENV=${NODE_ENV}
      - NEXT_PUBLIC_GITHUB=${NEXT_PUBLIC_GITHUB}
      - NEXT_PUBLIC_MAIL=${NEXT_PUBLIC_MAIL}
      - API_KEY=${API_KEY}
      - API_URL=${API_URL}
      - FRONTEND_PORT=${FRONTEND_PORT}
    depends_on:
      - backend
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

  backend:
    build:
      context: .
      dockerfile: docker/nestjs/prisma/Dockerfile
      args:
        - APP=backend
        - TAG=base-latest
        - NODE_ENV=production
    image: lbriza/coincrusade-runner-game:be-prod
    container_name: coin-crusade-game_be-prod
    restart: always
    ports:
      - ${BACKEND_PORT}:3001
    networks:
      - app-prod
    environment:
      - API_KEY=${API_KEY}
      - BACKEND_PORT=${BACKEND_PORT}
      - DATABASE_URL=${DATABASE_URL}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
      - DATABASE_NAME=${DATABASE_NAME}
      - POSTGRES_HOST=${POSTGRES_HOST}
      - MAIL_RECIPIENT=${MAIL_RECIPIENT}
      - MAIL_MODULE_HOST=${MAIL_MODULE_HOST}
      - MAIL_MODULE_USER=${MAIL_MODULE_USER}
      - MAIL_MODULE_PASSPHRASE=${MAIL_MODULE_PASSPHRASE}
      - NODE_ENV=${NODE_ENV}
    depends_on:
      - database
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

  database:
    build:
      context: .
      dockerfile: docker/postgres/Dockerfile
    image: lbriza/coincrusade-runner-game:db-prod
    container_name: coin-crusade-game_db-prod
    restart: always
    ports:
      - ${DATABASE_PORT}:5432
    environment:
      - NODE_ENV=${NODE_ENV}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_DB=${DATABASE_NAME}
    networks:
      - app-prod
    volumes:
      - ${DATABASE_PATH}:/var/lib/postgresql/data
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'
